# Naive-Discrete-Fourier-Transform
A fun little hand-coded implementation of a Fourier Transform. A low-frequency toy signal is defined beforehand over an interval of [0,1], and a much larger sampling rate is used. The goal was to turn math on paper into a code implementation that could reliably recover the frequencies present in the signal. 

